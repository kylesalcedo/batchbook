const crypto = require('crypto')

//API Info
API_KEY = API_KEY


function generate_auth_header(api_key) {
  
  const http_basic_payload = `${api_key}:`
  const http_basic_credentials = Buffer.from(http_basic_payload).toString('base64')

 
  //return http_basic_credentials
  console.log(http_basic_credentials)
}

generate_auth_header(API_KEY)